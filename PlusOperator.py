import Operand
import Operator

class PlusOperator(Operator):
    self.name = ""

    def evaluate(operand1,operand):
        if self.doesCompute()
        return operand1 + operand2
    
    def doesCompute(operand1,operand2):
        return self.doubleCheck(operand1,operand2,Operand.NUMBER or self.doubleCheck(operand1,operand2,Operand.STRING